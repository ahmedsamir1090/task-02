// Our movie model
class MainScreen {
  final String title, poster, route;

  MainScreen({
    required this.poster,
    required this.title,
    required this.route,
  });
}

// our demo data movie data
